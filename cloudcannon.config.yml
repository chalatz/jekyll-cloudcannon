collections_config:
  posts:
    icon: book
    add_options:
      - name: Add Post
        editor: content
        collection: posts
    schemas:
      default:
        path: schemas/post.md
    _inputs:
      date:
        instance_value: NOW
  drafts:
    icon: bookmark_border
    add_options:
      - name: Add Draft
        editor: content
        collection: draft
    schemas:
      default:
        path: schemas/draft.md
    _inputs:
      date:
        instance_value: NOW
  resource_pages:
    schemas:
      default:
        name: 'Resources page'
        path: schemas/resource.md
        icon: 'Menu Book'
